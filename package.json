{
  "name": "parcel",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "watch": "parcel src/index.html -d serve",
    "prebuild": "rmdir dist /s /q",
    "build": "parcel build src/index.html -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "zlchen",
  "license": "ISC",
  "keywords": [],
  "description": "",
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "rimraf": "^3.0.2"
  }
}
